import pygame
from scene import Scene
from choice import Choice

class Controller: 
    def init(self):
        """Initialzes controller, sets up pygame, start screen"""
    
    def mainloop(self):
        """Takes in the other models, such as scene and choice, and bridges them in order to move the story forward"""